# Frontend Note

This project uses a hybrid/server-rendered frontend with Thymeleaf templates in `backend/src/main/resources/templates` and static assets in `backend/src/main/resources/static`.

This folder is kept to match the required repository structure. If the team later migrates to React or Vue, the client application can be placed here.
